import { RayCanvas } from '../canvas';
import { Header } from '../interface';

const RayPage = () => {
    return (
        <>
            <Header />
            <RayCanvas />
        </>
    );
};

export default RayPage;
